# TODO

- [x] Update `netlify.toml` so root `/` serves `index.html`.
- [ ] Update `learn.html` with additional CSS polish (mobile spacing, header/background consistency).
- [ ] Update `practice.html` with additional CSS polish (mobile spacing, header/background consistency).
- [ ] Update `profile.html` with additional CSS polish (mobile spacing, header/background consistency).
- [ ] Manual verification: open `/`, `learn.html`, `practice.html`, `profile.html` in browser.

