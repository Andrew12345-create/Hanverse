[build]
  command = "npm run build"
  publish = "."
  functions = "functions"
  functionsNodeVersion = 18

[build.environment]
  NODE_VERSION = "18"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/server/:splat"
  status = 200
